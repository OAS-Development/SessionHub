import { NextRequest, NextResponse } from "next/server"; export async function POST(request: NextRequest, { params }: { params: { sessionId: string } }) { return NextResponse.json({ error: "Database functionality temporarily disabled during deployment" }, { status: 503 }); }
