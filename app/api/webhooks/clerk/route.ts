import { NextRequest, NextResponse } from "next/server"; export async function POST(request: NextRequest) { return NextResponse.json({ error: "Webhook functionality temporarily disabled during deployment" }, { status: 503 }); }
